[project]
name = "terrace"
version = "0.1.0"
description = "Plataforma de dados analíticos de futebol"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
    "pytest>=9.1.1",
    "statsbombpy>=1.22.0",
]
